name: SaathiCare Backend Test Verification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  verify:
    name: Verify Backend Test Reports
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openpyxl

      - name: Publish Test Results to Job Summary
        run: |
          python scripts/publish_backend_results.py

      - name: Upload Backend Test Report
        uses: actions/upload-artifact@v4
        with:
          name: Backend-Test-Report
          path: "REPORTS/backend/backend saati care result.xlsx"
